# ADR-0001: 모바일 플랫폼 선택

| 항목 | 내용 |
|------|------|
| **상태** | 승인(Accepted) |
| **결정일** | 2026-05-19 |
| **결정자** | kang3019 |

---

## 컨텍스트

**Slay the Flutter**는 Android/iOS 양 플랫폼을 대상으로 하는 턴제 덱빌딩 로그라이크 카드 게임이다. 초기 프로토타입부터 최종 제출까지 10~15주 내에 완성해야 하는 대학 과제 프로젝트로, 다음 제약 조건이 존재한다:

- 개발자 1인 (풀스택 · 게임 로직 · UI 모두 담당)
- Android · iOS 동시 지원 필수
- 제한된 개발 기간 — 빠른 이터레이션 필요
- Layered Architecture + 상태 관리 아키텍처 적용 요구
- 로컬 영속성 (레벨업·해금 데이터 저장) 필요

아래 5가지 플랫폼 선택지를 비교 검토하였다.

---

## 검토된 선택지

### 1. Flutter (Dart)

| 구분 | 내용 |
|------|------|
| **장점** | 단일 코드베이스로 Android · iOS 동시 빌드; 위젯 렌더링 엔진 내장으로 풍부한 커스텀 UI; Riverpod 등 성숙한 상태 관리 생태계; Hot Reload로 빠른 이터레이션 |
| **단점** | Dart 언어 신규 학습 필요; 앱 바이너리 크기가 네이티브 대비 다소 큼 |

### 2. React Native (JS/TS)

| 구분 | 내용 |
|------|------|
| **장점** | JS/TS 기존 경험 재활용 가능; 빠른 프로토타이핑; 넓은 npm 생태계 |
| **단점** | JS Bridge 오버헤드; 네이티브 모듈 의존 시 플랫폼별 설정 복잡; 카드 애니메이션 등 커스텀 렌더링에 제약 |

### 3. Android (Kotlin) 단독

| 구분 | 내용 |
|------|------|
| **장점** | 풀네이티브 성능; Android 도구 생태계 성숙 |
| **단점** | iOS 지원 불가 — 양 플랫폼 요구 사항 미충족; iOS 버전 별도 구현 시 공수 2배 |

### 4. iOS (Swift) 단독

| 구분 | 내용 |
|------|------|
| **장점** | Apple 생태계 깊이 있는 활용; 네이티브 성능 |
| **단점** | Android 지원 불가 — 양 플랫폼 요구 사항 미충족; macOS 개발 환경 필수 |

### 5. Kotlin Multiplatform (KMP)

| 구분 | 내용 |
|------|------|
| **장점** | 비즈니스 로직 코어 공유 가능; Kotlin 기존 경험 활용 |
| **단점** | UI 레이어는 플랫폼별 별도 구현 → 네이티브 UI 복잡도 높음; 생태계·문서 성숙도 Flutter 대비 낮음; 1인 개발 + 단기 일정에 부적합 |

---

## 결정

**Flutter (Dart)** 를 선택한다.

핵심 근거:

1. **양 플랫폼 동시 지원** — 단일 코드베이스로 Android·iOS를 커버, 1인 개발 공수 최소화
2. **커스텀 카드 UI 친화적** — Flutter의 독자 렌더링 엔진은 네이티브 컴포넌트에 의존하지 않아 카드 애니메이션·레이아웃을 자유롭게 구현 가능
3. **Riverpod 기반 Layered Architecture** — `Notifier` / `AsyncNotifier` API가 계층 분리 요구 사항과 자연스럽게 매핑됨
4. **Hot Reload** — 짧은 개발 사이클에서 UI 이터레이션 속도를 크게 높여줌
5. **Dart 학습 비용 수용 가능** — 강타입 언어 경험이 있으면 Dart 문법 적응에 1~2일이면 충분하고, 이후 생산성 이득이 이를 상쇄함

---

## 결과 (Consequences)

### 긍정적

- `lib/presentation/`, `lib/application/`, `lib/domain/`, `lib/data/` 4-레이어 Layered Architecture 구조를 언어 수준에서 강제할 수 있다.
- `flutter analyze` 정적 분석 + `flutter test` 단위 테스트 파이프라인이 즉시 사용 가능하다.
- 향후 웹·데스크톱 확장 시 동일 코드베이스 재활용 가능.

### 부정적 / 감수하는 트레이드오프

- Dart 언어 신규 학습 비용 발생 (초기 1~2주 생산성 저하 예상).
- 앱 바이너리 크기가 네이티브 앱 대비 크다 (카드 게임 규모에서는 실사용 문제 없음).
- 고성능 3D 그래픽이 필요할 경우 Flutter 렌더러 한계에 도달할 수 있으나, 본 프로젝트는 2D 카드 UI이므로 해당 없음.

---

## 관련 문서

- `.planning/00-vision.md` — 기술 스택 결정 컨텍스트
- `.planning/01-requirements.md` — MoSCoW 기능 요구 사항
- `AGENTS.md` — Layered Architecture·Riverpod 코딩 원칙
